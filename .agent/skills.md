# Skills

- Module-first backend changes using the existing `app/Modules/<Domain>` structure
- FormRequest to `Data` DTO to service/workflow delivery flow
- Repository-backed persistence and query discipline
- API resource response-shape consistency
- AI workflow changes across `app/AI`, `app/Modules/Ai`, and queued jobs
- MCP tool and prompt work under `app/Mcp`
